name: CDK Diff

on:
  issue_comment:
    types: [created]

jobs:
  run-cdk-diff:
    if: >
      github.event.issue.pull_request != null &&
      github.event.comment.body == '/cdk-diff'
    runs-on: ubuntu-latest

    steps:
      - name: Debug - Triggered Event
        run: |
          echo "Event Name: ${{ github.event_name }}"
          echo "Issue Number: ${{ github.event.issue.number }}"
          echo "Comment Body: '${{ github.event.comment.body }}'"
          echo "Is Pull Request: ${{ github.event.issue.pull_request != null }}"
      
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18' # Adjust as needed

      - name: Install Dependencies
        run: npm install

      - name: Run CDK Diff
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: npx cdk diff

      - name: Comment Success
        if: success()
        uses: actions/github-script@v6
        with:
          script: |
            github.issues.createComment({
              issue_number: ${{ github.event.issue.number }},
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "✅ CDK Diff completed successfully. You can now run `/cdk-deploy` to deploy."
            })

      - name: Comment Failure
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            github.issues.createComment({
              issue_number: ${{ github.event.issue.number }},
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "❌ CDK Diff failed. Please check the workflow logs for details."
            })
